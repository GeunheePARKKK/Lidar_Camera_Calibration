# session_020 — 최종 검증

2026-09-15 KST, 카메라 1대(CAM1, S/N FHH26070137) + 라이다, 20초. PPS·Phase Lock·GPRMC·카메라 Line1이 모두 동작한 상태로, `check_session.py`의 전 항목을 통과한 세션입니다 (카메라 원본 이미지를 넣은 원래 위치 기준).

| 파일 | 내용 |
|---|---|
| `lidar.pcap` | 라이다 UDP 원본 21 MB |
| `mcu_log.txt` | ESP32 로그 — 트리거 200회(`T`), PPS·GPRMC 22회, 노출 엣지(`EXP`) 10회, `CNT,200,200,0,0,0` |
| `cam1/frames.csv` | 카메라 200장의 FrameID와 수신 시각 (원본 이미지 490 MB는 제외) |
| `meta.json` | 수집 설정과 라이다 설정·상태 스냅샷 |
| `sync_report.txt`, `check_session.txt` | 도구 출력 |
| `preview.png` | 카메라 + 라이다 평면도·측면도 |

## 재현

```bash
cd ../../code
python3 sync_report.py ../results/session_020
python3 check_session.py ../results/session_020
```

`check_session.py`는 이 사본에 이미지 파일이 없어서 카메라 `files` 칸이 0이고 "문제 있음"으로 끝납니다. 그 외 항목(트리거·노출 200/200, 사진 번호 대응 200/200, 라이다)은 그대로 검산됩니다.

## 핵심 수치

| 항목 | 값 |
|---|---|
| 셔터(트리거) 순간 라이다 방위각 | 평균 −0.007°, 표준편차 0.339°, 범위 −0.92 ~ +0.92° |
| 시간 오차 | 평균 −0.000002 s, 표준편차 0.000094 s, 최대 0.000257 s |
| 카메라 트리거 → 노출 시작 | 12 ~ 14 µs, 평균 12.8 µs (표본 10개) |
| 노출 시작 기준 방위각 | 평균 +0.039°, 표준편차 0.339° (시간 오차 평균 +0.000011 s) |
| 라이다 단독 흔들림 | ±93 µs (0° 통과 위치 0.00 ms) |
| 트리거 ↔ 노출 / 사진 번호 대응 | 200 / 200, offset 1 → 200 / 200 |
| GPRMC | 2,916건 — 초 일치 2,711건, 새 문장 도착 전(PPS 후 70 ms 이내) 205건 |
| PPS | `Locked` 전 구간 |
