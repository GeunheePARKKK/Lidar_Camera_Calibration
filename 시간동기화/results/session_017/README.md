# session_017

2026-09-15 00:11 KST, 카메라 1대(CAM1, S/N FHH26070137) + 라이다, 20초. PPS·Phase Lock·GPRMC가 모두 동작한 상태의 최종 검증 세션입니다.

| 파일 | 내용 |
|---|---|
| `lidar.pcap` | 라이다 UDP 원본 21 MB (데이터 15,844 패킷 + 위치 패킷) |
| `mcu_log.txt` | ESP32 로그 — 트리거 200회(`T`), PPS 22회, GPRMC 22회 |
| `cam1/frames.csv` | 카메라 200장의 FrameID와 수신 시각 (원본 이미지 490 MB는 제외) |
| `meta.json` | 수집 설정과 라이다 설정·상태 스냅샷 |
| `sync_report.txt` | `sync_report.py` 출력 |
| `preview.mp4`, `preview.png` | 카메라 + 라이다 평면도·측면도 |

## 재현

```bash
cd ../../code
python3 sync_report.py ../results/session_017
```

`check_session.py`는 이미지 파일(`*.npy`)이 없어서 카메라 항목을 "파일 수 불일치"로 표시합니다. 라이다와 ESP32 항목은 그대로 검산됩니다.

## 핵심 수치

| 항목 | 값 |
|---|---|
| 셔터 순간 라이다 방위각 | 평균 +0.015°, 표준편차 0.380°, 범위 −0.93 ~ +1.22° |
| 시간 오차 | 평균 +0.000004 s, 표준편차 0.000106 s, 최대 0.000338 s |
| 라이다 0° 통과 위치 | 99.99 ms (기대 0 ms, 차이 −10 µs), 흔들림 ±110 µs |
| GPRMC | 2,915건 — 초 일치 2,706건, 새 문장 도착 전(PPS 후 70 ms 이내) 209건 |
| 카메라↔라이다 수신 시각 차 | 15.1 ms 고정 (노출 8 ms + 전송 7.1 ms) |
| PPS | `Locked` 2,915 / 2,915 |
